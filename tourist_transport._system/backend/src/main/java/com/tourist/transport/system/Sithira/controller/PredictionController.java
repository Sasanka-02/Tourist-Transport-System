/*package com.tourist.transport.system.Sithira.controller;
@CrossOrigin(origins = "http://localhost:3000")  // Above class declaration
@RestController
@RequestMapping("/api")

public class PredictionController {
    @GetMapping("/predictions")
    public List<HistoricalData> getPredictions(@RequestParam String district) {
        return List.of(
            new HistoricalData("Galle", "Germany", LocalDate.now(), 7, 320)
        ); // Mock data for now
    }
}*/